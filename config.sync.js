const config = require('./config');

const syncConfig = module.exports = {};

syncConfig.email = 'CHANGEME';

syncConfig.password = 'CHANGEME';

syncConfig.userDir = config.baseDir + 'users/';

syncConfig.remote = 'CHANGEME'
